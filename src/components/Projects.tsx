import React from 'react';
import { Box, Typography } from '@mui/material';

const Projects = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Projects</Typography>
      <Typography>This is the projects section.</Typography>
    </Box>
  );
};

export default Projects;
